# N64Recomp config for streamed-overlay bank unit AB (Phase 4).
# See config/banks/config-bankR.toml for the model. Record 9 arena bank:
#   ROM 0x1977b0 (0x4f80) -> RAM 0x80214FA0 (see config/banks/config-bankAB.yaml).

[input]
output_func_path = "../../BankABFuncs"
elf_path = "../../build/bankAB.elf"
rom_file_path = "../../assets/ogre64.z64"
use_absolute_symbols = true
uses_mips3_float_mode = true
bss_section_suffix = ".bss"
functions_per_output_file = 100

function_sizes = []
