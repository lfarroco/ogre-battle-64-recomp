# Documentation

Technical documentation for the Ogre Battle 64 (USA, Rev A) PC port project.

| Document | Purpose |
|---|---|
| [../AGENTS.md](../AGENTS.md) | **Read this first if you are an AI agent**: working rules for this repo — ask the developer what the current scene should display rather than inferring intent, treat handoffs as hypotheses, verify the consumer before naming a struct, isolate one variable per experiment, revert probes by regenerating. |
| [DECISIONS.md](DECISIONS.md) | Living log of technical decisions. **Read the "Durable decisions" table at the top first** — it is the decisions that still bind the port, newest first; the entries below are session reasoning (superseded ones carry a banner). |
| [scenes.md](scenes.md) | **What each screen is supposed to show**, sourced from the developer (the AGENTS §1 oracle), with the port's status per screen and the New Game opening's five parts. |
| [symbols.md](symbols.md) | Proposed symbol names: address → name → evidence → confidence, plus the naming convention and what a rename costs. Read before naming a function or calling one "the X function". |
| [STATUS-LOG.md](STATUS-LOG.md) | The session-by-session status log that `PLAN.md` carried from session 3 to session 97, moved there verbatim. `PLAN.md` holds the live status and the open-work list. |
| [HANDOFF-2026-08-24.md](HANDOFF-2026-08-24.md) | Session handoff: current state, the first-boot SIGBUS, and next steps. |
| [HANDOFF-2026-08-25.md](HANDOFF-2026-08-25.md) | Session 4 handoff: first RSP task + real VI mode achieved; drainer thread; next steps. |
| [HANDOFF-2026-08-25-session5.md](HANDOFF-2026-08-25-session5.md) | Session 5 handoff: RT64 renderer integrated; game stalls after its first RSP task; F3DEX 2.08 short-format GBI mismatch. |
| [HANDOFF-2026-08-25-session6.md](HANDOFF-2026-08-25-session6.md) | Session 6 handoff: the stall was a scheduler busy-spin deadlock; 3 fixes (spin, PI DMA, byte order) unblock boot into the main loop; Phase 4 (streamed overlays) starts. |
| [HANDOFF-2026-08-25-session7.md](HANDOFF-2026-08-25-session7.md) | Session 7 handoff: Phase 4 begins — overlays are plain linked code (no relocation); streamed overlays A+B+C recompiled and registered; boot passes the old crash and reaches the next overlay's data load, then spins on N64 threads 1+3. |
| [HANDOFF-2026-08-25-session8.md](HANDOFF-2026-08-25-session8.md) | Session 8 handoff: the post-boot spin was a cooperative-scheduler deadlock; N64Recomp now emits `yield_self` for poll loops; boot reaches the title-screen display-list build (next crash in `func_8019FC68`). |
| [HANDOFF-2026-08-25-session9.md](HANDOFF-2026-08-25-session9.md) | Session 9 handoff: the `func_8019FC68` crash was a KMC shared-epilogue recompilation bug; N64Recomp now emits fall-through tail calls (with a static-function fixpoint); boot reaches real RT64 rendering (next crash in the RT64/Vulkan render thread). |
| [HANDOFF-2026-08-29-session10.md](HANDOFF-2026-08-29-session10.md) | Session 10 handoff: VI-thread crash fixed; GBI question resolved (F3DEX2 auto-detection is correct); stable under Lavapipe. |
| [HANDOFF-2026-08-29-session11.md](HANDOFF-2026-08-29-session11.md) | Session 11 handoff: WebAssembly port kickoff — RT64 optional, null renderer, web shell; native macOS boot fixed (stale ELF/funcs, missing runtime fixes, osSpGetStatus bridge, broken debug probe). |
| [HANDOFF-2026-09-25-session103.md](HANDOFF-2026-09-25-session103.md) | **Latest.** Session 103 handoff — **the `EXP Overflow` mod is `mods/exp-overflow/`, and it carries a level-up's leftover EXP over.** The game levels a character at 100 EXP and the result loop `func_ovlJ_801D61C0` (state 4 at 0x801D634C) zeroes the participant's EXP (`sb zero,50(s0)` in the delay slot of `jal func_ovlJ_801D8E90`) before calling the base level-up routine `func_8016EBC4`. Neither can be hooked: the result loop is in a streamed bank (the port's `get_vrom_to_section_map()` covers only the base sections), and `func_8016EBC4` faults during boot when hooked (`Failed to find function at 0x46220003`), while its neighbours hook cleanly. The mod therefore hooks the base frame function `func_80072944` (verified once per frame: 3072 entries / 3203 frames), remembers `participant+0x32 - 100` against the roster index `participant+0xF6`, and once the record shows `level+1` writes the remainder into `record+0x35` **and** the live `participant+0x32` (a record-only write is undone by the next sync). Confirmed on the developer's suspend save: four front-row soldiers at 89 EXP reached level +1 at **20 EXP**, and 20 survived the next battle; without the mod they end at 0. |
| [HANDOFF-2026-09-25-session102.md](HANDOFF-2026-09-25-session102.md) | Session 102 handoff — **WIDESCREEN is a SETTINGS row with `OFF` / `MISSIONS` / `ALWAYS`, and it is a port feature and not a mod.** `app/src/widescreen.cpp` (new) is the only writer of RT64's `ar_option`: in `MISSIONS` mode it turns Expand on only while the dispatcher's active scene is `0x03` (the mission), and `update_gfx` calls it once per frame beside `poll_scene()`. RT64 already did the widening (`ProjectionProcessor::processScene` counter-scales the game's projection matrices, so the 3D field is hor+ and not stretched), and the port already forwarded `config.ar_option` and handled a runtime change. `settings.{hpp,cpp}` gained the mode, the `widescreen` key in `settings.cfg` and `OGRE_WIDESCREEN`; the GAME SPEED radio layout was generalised (`layout_radio`) so both rows share one measure/draw/click geometry. **The window shape follows the mode and never the scene** (developer correction): `off` opens 4:3 with no pillarbox bars, `missions`/`always` open 16:9 and the 4:3 scenes are pillarboxed inside it; only changing the mode re-fits the window. The large 2D backgrounds cannot show more to the sides: their art is 4:3 (widest asset 336x256) and the cathedral's final blit loads the source's full width, so only 3D gains real width. Verified with the session-98 suspend save (`assets/escort_suspend.bin`): `expand` on every `0x03` visit and `original` on each `0x02`/`0x0D` between them, `off` logging no transition, and the same mission field pillarboxed 4:3 vs full 16:9 (`docs/proofs/native-widescreen-*.png`). The `MISSION` banner row clips in 4:3 too (the game's own marquee), and `ALWAYS` stretches the boot's full-screen 2D stills. |
| [HANDOFF-2026-09-25-session101.md](HANDOFF-2026-09-25-session101.md) | Session 101 handoff — **the unit profile's DEF/MDEF "extra digit" was RT64's "Fix LR with Scissor" enhancement rewriting the digit's rectangle, now off by default.** The developer's retail cutout of the same panel is clean. Scene `0x06` is one F3DEX2 display list (`0x80257E50`); each digit is a 6x7-px `G_TEXRECT` at one texel per pixel (`28` = `(288,66)-(294,73)` s=48 plus `(281,66)-(287,73)` s=12), and the font sheet (`0x801FFCF8`, 64x64 CI4) has the digits in 6-texel cells, so the dark column at `x=294` is cell 9's column 0 after `8` (`11` → cell 2's column 0 after `1`). The panel scissor is `(100,84)-(1180,612)` and `RDP::drawRect` (`rt64_rdp.cpp`) does `if ((abs(scissorRect.lrx - lrx) <= 4) && (ulx < scissorRect.lrx)) lrx = scissorRect.lrx;` under `enhancementConfig.rect.fixRectLR`: `abs(1180 - 1176) == 4`, so the 6-px glyph rectangle became 7 px and sampled texel 54. A temporary `probe101` trace printed the draw call as `raw 1152,264..1180,292 cycle=0 dsdx=1024` where the display list word in the same dump is `lrx=1176`, ruling out the copy-mode `lrx |= 3` (which would print `dsdx=256`) and any tile-window limit (`t.lrs=252` = 63 texels); parallel-rdp has no such enhancement, which is why the emulator is clean. Fix: `app/src/renderer.cpp` clears `fixRectLR` after `app_->setup(...)` (`OGRE_RECT_LR_FIX=1` A/Bs it). Verified: probe `w=6`, three fixed dumps show column 294 as border, **developer-confirmed**. New tooling: console `snap [prefix] [ms]` = parked-thread RDRAM image plus RT64 present captures. Probe reverted; `tools/RT64` diff matches `patches/rt64-ob64.patch`. |
| [HANDOFF-2026-09-25-session100.md](HANDOFF-2026-09-25-session100.md) | Session 100 handoff — **the Linux release had no audio because its statically linked SDL2 had only the OSS driver.** Issue #8: 0.3.0 on CachyOS KDE and on a fresh Fedora box both print `[SDL] audio unavailable, running silently: dsp: No such audio device`. That string is SDL2's OSS driver (`SDL_dspaudio.c:293`), and `strings` on the released Linux binary shows `OSS /dev/dsp standard audio` with no ALSA, PulseAudio or PipeWire: the package's SDL2 2.32.10 was built on a host without the audio development headers, and SDL2's CMake drops such a backend without failing (the configure summary says `SDL_ALSA (Wanted: ON): OFF` while `CMakeCache.txt` still reads ON, so only the built archive is a check). Reproduced in a bare `ubuntu:24.04`: `libSDL2.a` defines `DSP`/`DUMMYAUDIO`/`DISKAUDIO_bootstrap` without the headers and `ALSA`/`PULSEAUDIO`/`PIPEWIRE_bootstrap` with them. Landed: `tools/check-sdl2-audio.sh` (new), `make sdl2-static` verification plus rebuild of a cached prefix that fails, a Linux audio-backend check in `tools/smoke-dist.sh` (it fails on the real 0.3.0 binary), the three `-dev` packages in the hosted Linux CI step, and the compiled-in driver list in the app's audio-failure line. No game or runtime code changed. Open: the 0.3.0 Linux archive needs rebuilding and republishing, and no Linux run with sound was possible on this machine. |
| [HANDOFF-2026-09-25-session99.md](HANDOFF-2026-09-25-session99.md) | Session 99 handoff — **the C buttons produced the wrong directions because their N64 bit masks were rotated one position.** The developer's `I → right, J → top, K → left, L → down` is `app/src/input_map.hpp`'s four C values, which must be libultra's `CONT_C_UP/DOWN/LEFT/RIGHT` = `0x0008/0x0004/0x0002/0x0001` because `osContGetReadData` writes the callback word straight into `osContPad.button`; the key table already assigned `I/J/K/L` to the right rows, so only the values changed, and `app/web/web.js` carried its own copy of the same rotation. |
| [HANDOFF-2026-09-25-session98.md](HANDOFF-2026-09-25-session98.md) | Session 98 handoff — **the escort mission's slow field is the session-80 class a third time.** The developer's battery save (a 32768-byte raw `QuestOG3` image resuming at scene `0x03` through the title → `0x12` Load Game suspend route) reproduced only on the fourth profiled run: the mission field's display-list periods go from 8 ms to **80–183 ms for 2.6 s** (`t=57304..59921 ms`, no scene change), `[prof] T=59000` puts the frame-pump thread `t4` in **`0x801D0B78` at 93 %**, and the snapshot repeats `hotloop t4: 0x801D0B78 x100`. `func_ovlN_801D0B78` (bank unit N, record 7) scans the `-1`-terminated object-id list at `obj+0xA8`; N64Recomp injected a blocking `yield_self` at its `bnel` skip branch `0x801D0C08`, so it ran once per skipped entry. `config/banks/config-bankN.toml` now lists that branch in `yield_work_loop_branches`; regeneration removes exactly one site (50 → 49). The new durable part is the ownership check, because `0x801D0B78` is RAM shared by five bank units and the profiler prints a function entry: `bank_funcs.inc` registers it only for unit N, the run's last load there before the stall is record 7 (`t=51984`), and unit AH never loaded and has no `yield_self` at all. Fixed build, same save, 180 s: the address in **0 of 180** `[prof]` t4 lines (pre-fix 4 of 61), longest run of consecutive ≥ 50 ms gaps **20 → 2**, `[cover] 0x801D0B78 276` so the scan still executes; `check-banks` OK, `elf-rom-check` 0 differing bytes, boot `runlog --check` PASS. **Developer-confirmed**, and the same fix removed a second symptom of the same site: the slowdown when **opening the list of units**, which also makes the unit list a deterministic trigger for this class. |
| [HANDOFF-2026-09-22-session97.md](HANDOFF-2026-09-22-session97.md) | Session 97 handoff — **the repo root is reorganised: every config the build reads is under `config/` (`config.{yaml,toml}`, `config/banks/config-bank<U>.{yaml,toml}`, `config/symbols/**`, `config/rsp-*.toml`) and the upstream patches under `patches/`; the root dropped from 181 entries to 64.** splat, N64Recomp and RSPRecomp resolve a config's paths against that config's own directory, so the configs carry `../` entry points and each bank yaml sets `base_path: ../..`. Removed: the 10 `.ogre-prefs-*` sandboxes (400 MB), `.repro/`, `.DS_Store`, the tracked `ogrebattle64.d` (now gitignored) and the unreferenced `ogrebattle64.yaml`. Verified: `make regenerate` exits 0 and leaves `asm/`/`ogrebattle64.ld` byte-identical, the main `RecompiledFuncs/` unchanged, `check-banks` OK, and each of the 11 bank units whose output changed (B..L) re-links byte-identically from its `HEAD` config. The move also exposed that the tree's `build/bank{B..L}.elf` were stale; the relink is why `Bank{B..L}Funcs/` now match a fresh regeneration. `docs/HANDOFF-*.md` keep the old paths. |
| [HANDOFF-2026-09-21-session96.md](HANDOFF-2026-09-21-session96.md) | Session 96 handoff — **the defeat crash localised, and the title sprite's green border localised. No fix landed for either; every probe reverted.** (1) The SIGBUS after a lost battle is `func_800862C0+0x59A`, the `lw $s6, 4($a1)` at guest `0x80086328`; `$a1` is `+0x7C` of an effect object, the value looks like a misread record field (`0xFFF50000` = -11.0 as 16.16), and the crash lands on a scene/mode transition. The effect subsystem has no cross-bank call site, so the writer is another resident function; two developer reproductions did not crash. (2) The title sprite's green border is the blend factor, not the texture: the I8 mask and the combiner are byte-exact, but the draw composites at alpha 1 (`resultAlpha.a = combinerColor.a;` in `RasterPS.hlsl` removes it). The defect is present in every source state buildable here, including RT64 at the 2026-09-17 pin, so it is in the renderer or presenter path. |
| [HANDOFF-2026-09-21-session95.md](HANDOFF-2026-09-21-session95.md) | Session 95 handoff — **the `ESC` overlay's DEBUG tab shows the live Chaos Frame, and the address is `0x801936C9`, not the community `0x801936A9`.** The initialiser `func_8016C900` bzeroes the flags block at `0x80193698` and stores `50` at `0x801936C9`; the scene-`0x13` update `func_801B5128` reads and masks it, and the script VM opcode `0xFF` stores a script operand there. `0x801936A9` has no static read or write and reads 0 at the title. The shared `ui::Panel` does not read guest memory: the overlay samples the byte each frame and pushes it in, so the launcher's DEBUG tab shows `GAME NOT RUNNING`. Row bands (developer): `LOW` 0-35, `NEUTRAL` 36-64, `HIGH` 65-100; `OGRE_OVERLAY_TAB=debug` opens it for a capture. |
| [HANDOFF-2026-09-20-session94.md](HANDOFF-2026-09-20-session94.md) | Session 94 handoff — **the distributed build opens no console window and writes `error.log` on a crash; the Windows `0xC0000005` is RT64's renderer setup on an old GPU; closing the window no longer freezes.** macOS ships `Ogre Battle 64.app` and `SDL_FILESYSTEM_BASE_DIR_TYPE=parent` keeps the ROM, saves, mods, `controls.cfg`, `settings.cfg` and `error.log` in the package folder; Windows links a GUI-subsystem `.exe`, and `app/src/crash_log.cpp` tees stdout/stderr into 128 KiB rings and writes `<config dir>/error.log` with the fault, the host pc and the last log lines. The v0.2.0 Windows build died silently because `setvbuf(stdout, nullptr, _IOLBF, 0)` is an invalid-parameter fast-fail in the MSVC CRT, and the package shipped none of the four CRT DLLs its DXC binaries import. `tools/smoke-dist.sh` now fails a package whose crash report lacks the captured boot line. The access violation is open: the console build printed RT64's Vulkan-workaround line, so `OGRE_GRAPHICS_API` pins the backend and the next report names the faulting module. |
| [HANDOFF-2026-09-20-session93.md](HANDOFF-2026-09-20-session93.md) | Session 93 handoff — **a SETTINGS tab carries GAME SPEED, changeable while playing.** The shared `ui::Panel` gained a fifth tab whose only row is a radio group over **1, 2 and 4** (6 and 8 were dropped: the developer reports cursor aiming cannot keep up). The value is the runtime's emulated-clock multiplier, persisted at `<config>/settings.cfg` (`app/src/settings.{hpp,cpp}`), and `OGRE_SPEED` overrides the file for one run without writing it. `ultramodern::set_speed_multiplier()` publishes the clock's segment anchors under a seqlock, so `osGetCount`/`osGetTime` and pending `OSTimer` deadlines stay continuous across a change; a mutex on that read path cost the boot visible time. |
| [HANDOFF-2026-09-20-session92.md](HANDOFF-2026-09-20-session92.md) | Session 92 handoff — **the start screen is tabbed (START GAME / ROM / MODS / CONTROLS) and `ESC` opens the same panel over the running game.** `app/src/input_map.{hpp,cpp}` holds one row per N64 button (a keyboard scancode plus `None`, a gamepad button or a right-stick direction), persisted at `<config>/controls.cfg` and read on every input poll; the defaults match the old hardcoded map. `app/src/ui.{hpp,cpp}` holds the shared panel and bitmap font, so the launcher and the overlay cannot drift. The overlay is a borderless always-on-top SDL window (opacity 0.90) drawn from `update_gfx`; `get_input` returns an idle pad while it is up. The game does not pause behind it (developer's choice). |
| [HANDOFF-2026-09-20-session91.md](HANDOFF-2026-09-20-session91.md) | Session 91 handoff — **the mod system is on and an example mod ships.** `GameEntry::mod_game_id` was never set, so `wait_for_game_started()` parsed every `.nrm` and discarded it; `app/src/main.cpp` now sets it and shows the start screen whenever a mod is installed. `mods/skip-boot-logos/` is the reference code mod: an entry hook on scene `0x09`'s update writes the title's id into `D_800C4C26`, and it ships **off** (`enabled_by_default = false`; `n64recomp-ob64.patch` adds the field to RecompModTool). The skip must come from `0x09`, not `0x0A`. Four runtime fixes (section-index translation, data-only HI16/LO16 relocations, the macOS `__TEXT` page remap for `patch_func`, and sign-extension in the `[snap]` queue snapshot) are in `n64modernruntime-ob64.patch`. |
| [HANDOFF-2026-09-20-session90.md](HANDOFF-2026-09-20-session90.md) | Session 90 handoff — **the "Use Item" unit-command screen runs at full speed.** The screen's per-frame lookup `func_ovlN_801DA248` (bank unit N, record 7) is a pure 10×5 table scan whose outer loop `is_yield_poll_loop` classified as a poll because the body loads `lw $v0, 0($a0)` from the never-written argument base `$a0` (`0x801DA268`); the injected blocking `yield_self` at branch `0x801DA2C0` ran on every iteration but the last, and the frame-pump thread `t4` sat there 99 % of every slow second with display-list periods of 237–275 ms. Fixed by appending `0x801DA2C0` to `config/banks/config-bankN.toml`'s `yield_work_loop_branches` (session 80's mechanism): one generated file changes, 51 → 50 yield sites, `check-banks` OK, boot unchanged. Fixed build: scene-`0x03` intervals p50 8 / p90 14 / p99 34 ms (was p99 209 / max 275), `0x801DA248` in no profiler sample, present in the coverage census so the screen was reached, developer-confirmed normal. Named but not landed: `func_ovlN_80204C08` (branch `0x80204C48`), the same shape. |
| [HANDOFF-2026-09-19-session86.md](HANDOFF-2026-09-19-session86.md) | Session 86 handoff — **the credits screen (scene `0x11`) plays.** Four `config/config.toml` `function_sizes` overrides were leaking the frame-pump thread's stack — the session-41 class, every violation an early-`return` `jal` into a body interior or a dropped `jr ra` delay slot: `func_801AB76C 0x22C`→`0x4` (swallowed the real `func_801AB770`, so `func_801AB998`'s `jal 0x801AB770`, the credits text drawer, leaked), `func_801AB568 0x1E0`→`0x208` (its epilogue continuation dropped the `addiu sp,sp,56` delay slot, leaking `0x38` per call), `func_801B00D0 0x544`→`0x4` (swallowed the real `func_801B00D4`, called by `func_801B1BBC` on the credits tree) and `func_801AFC2C 0x4A4`→`0x4A8` (its own delay slot, leaking `0x80`). Two static shape checks find the class — a `jal` emitted as call + `recomp_trace_return(); return;` with a duplicated delay slot (5→3 sites, all latent), and a `jr $ra` emitted with no delay slot (2→0) — plus a call-tree audit that compares each `jal` comment with the emitted callee. **Method correction: a forced `OGRE_SCENE=0x11` run does not execute the credits** (it plays the intro/opening even when the trace says `active=0x0011`); only the natural ending validates it. Verified from the developer's suspend save in front of the final boss (`assets/saves/suspend_final_boss.n64`, gitignored) at `OGRE_SPEED=8`: continuous display lists, `D_800AEFA4` tracking `D_800C4BCC`, the fixed functions executing, then the Chaos Frame scene `0x13` and the attract loop; 0 stubs, `check-banks`/`cross-bank-check` OK. Developer-confirmed; open: artifacts in the credit backgrounds, three latent leak-shaped `jal`s. |
| [HANDOFF-2026-09-19-session85.md](HANDOFF-2026-09-19-session85.md) | Session 85 handoff — **the streamed-module hunt is exhausted: there is no un-recompiled module on any reachable path.** Run as a hunt (a table-driven load has no loader `jal` to find): two hand-played sessions (~2 h emulated — tutorial incl. mode 2, mission incl. battles, map, Organize, Load Game) plus a forced sweep of every scene id, all with `OGRE_DMA_TRACE=1 OGRE_DMA_TRACE_FULL=1 OGRE_COVER`, produced **0** `UNKNOWN module`, **0** `UNCOMPILED streamed record`, **0** `streamed function stub called`, **0** stub-microcode tasks; an offline diff of every DMA pair in the censuses (179 177 + 171 758 + 42 937) against both registration tables found **0** unknown ROM at a known module base and **0** arena-shaped load with a known-arena delta at an unknown base; and every registered module executed ≥1 function (session 83's two 0% modules, units T and U, now both execute). The battle is a mission sub-state (unit `X` + `AH` load while `active=0x0003`), and the closing movie's unit `I` loaded under the forced `0x16`. Two hunt-diagnostic bugs fixed: `OGRE_SCENE_TRACE` walked the wild scene-object word at the title→story handoff (SIGBUS in `RT64Renderer::update_screen`; now bounds-checked and verified) and the window-close path now dumps the DMA census. Open: a teardown crash `func_8007F8E4 + 0x2F8` after the census dump. The only "not compiled" backlog left is static and latent — `stubmap`'s 129 missing-function candidates and 288 `jalr` sites. |
| [HANDOFF-2026-09-18-session83.md](HANDOFF-2026-09-18-session83.md) | Session 83 handoff — **the missing-stub whack-a-mole is now a tool, and its first answer is a negative that is worth having: every static `jal` target in the build is registered by some compiled module.** `tools/stubmap.py` (new) reads the runtime's *registration tables* — `app/src/bank_funcs.inc` (what `on_streamed_dma` registers) and `RecompiledFuncs/recomp_overlays.inl`'s section table (what `load_overlays(0x1000, entrypoint, 0x3E1B0)` installs, and the only place a reimplemented function such as `osSetIntMask_recomp` has an entry, since it has no generated body) — and every `jal` comment in the generated C, then buckets each of the **985** dispatched addresses: **DEFINITE** (no module registers it at all — no run state resolves it — or the caller's own bank lacks it inside its own DMA'd code), **MISSING FUNCTION** (a bank that can be resident has a real prologue there but no entry), **BANK SELECTION** (the missing bank's bytes are a body interior; scene data picks the bank), **INERT** (the record masks say the two records never load together), plus UNRESOLVABLE and the RSP-IMEM window. Current build: **0 definite, 0 unresolvable, 129 missing-function, 152 bank-selection, 369 inert, 332 resolvable; 288 indirect `jalr` sites are the true blind spot.** `stubmap.py log <run.log>` is the arbiter and needs no new instrumentation: the runtime already prints the live words at a stub address, so the tool matches them against each candidate's ROM image, names the module that *was* resident, and says whether the bug is a load that missed `on_streamed_dma` (the bank registers the address) or a wrong-layout dispatch (it does not, and the tool names the body it landed in). Validated by replaying the pre-session-82 tree, where `0x801D1508`/`0x801D0AAC` come out DEFINITE; by `ogre-s73-r5.log` (record 16 → unit AG) and `ogre-live4.log` (record 9ab → unit AB). `make stubmap` / `make stub-check`. |
| [HANDOFF-2026-09-18-session82.md](HANDOFF-2026-09-18-session82.md) | Session 82 handoff — **neutral encounters now spawn their monster: the battle setup fragment scene `0x0E`'s enter streams was the one bank nobody's route had run.** ROM `0x23A370` (`0xE80` → RAM `0x801D0860`) was uncompiled, so `jal 0x801D0AAC`/`0x801D1508` hit the runtime's streamed stub and the battle's participant table was never built — normal battles worked because their units already exist on the field, a neutral encounter's wild unit is *created* there, hence "instant victory with no enemy". Now **bank unit AH** (`bankRec10s`, siblings of units J and I in the same RAM, three forced entries). The trigger is fully decodable from the ROM (`func_ovlN_801E7940`, `rand() % 72000 <= 50`, terrain tables `0x801ED780`/`0x801ED79E`, class row `0x801E7E38 + mission*12`), so the rare event was reproduced on demand by patching two branches (probe82, reverted); `tools/arenamap.py` had listed the fragment since session 73 as "no observed run streams" — the tool was right, the route coverage was not. Developer-confirmed ("it worked"); `docs/proofs/native-neutral-encounter-message.png`, `-battle.png`. |
| [HANDOFF-2026-09-18-session81.md](HANDOFF-2026-09-18-session81.md) | Session 81 handoff — **the cutscenes' flickering right/bottom line is fixed, with two RT64 changes.** (1) A game framebuffer is now sized from the **VI**, not from the rectangle the frame drew (`framebufferHeightForDisplay`, `tools/RT64/src/hle/rt64_framebuffer.h`; used at `rt64_workload_queue.cpp:435`, `rt64_state.cpp:551`/`:1294`) — the RDP snaps a copy/fill rect's fractional bits (`lrx \|= 3; lry \|= 3`), so frames whose draws stopped at row 238 gave a 239-row target while others gave 240/241; measured: **320x240 in 1361/1361 and 2465/2465** consecutive frames (was 239/240 alternating), and the stale-edge line on the black frames after a checkpoint `load` is gone. (2) The presenter shows only the **319x239 the game actually draws** (`visibleFramebufferSize` in `tools/RT64/src/render/rt64_vi_renderer.cpp`, `OGRE_OVERSCAN=<0..8>`, default 1 px right/bottom) — `OGRE_DL_DECODE=all` shows the game's own scissor and clear are `(0,0)-(1276,956)` = 319x239, so its draws never touch the framebuffer's last column/row; those pixels keep leftovers from the boot's njpeg draws (present only in `0x400`, per a dump watch at t=4..12 s; the other two buffers stay black) and the VI's three-buffer rotation toggled them — **120 of 120 consecutive settled-cathedral frames are clean (was 15 of 40)**, the picture is intact, `OGRE_OVERSCAN=0` restores the old image, and boot/title/load-game/map plus the intro and Magnus cutscenes are unchanged (all runs exit 0). A CRT's overscan hid the game's own off-by-one. Reference model recorded (mupen64plus-core forwards raw VI registers; GLideN64/angrylion present a fixed window from `VI_ORIGIN` where RT64 subtracts a row; GLideN64's `enableTexCoordBounds` has no RT64 equivalent) and the `VI_ORIGIN` probe reverted. |
| [HANDOFF-2026-09-18-session80.md](HANDOFF-2026-09-18-session80.md) | Session 80 handoff — **the mission's post-battle lag is fixed, and with a one-site change.** Session 79's diagnosis is accepted: N64Recomp's poll-loop heuristic injects a blocking `yield_self()` into `func_ovlN_801B2F4C`'s nearest-target scan (branch `0x801B3008`), once per entity, and a `yield_self` blocks until an external message arrives (≈ one VI retrace). Because all three *broad* repairs of session 79 deadlocked the boot, the repair is **declarative and scoped**: a new N64Recomp option, `yield_work_loop_branches` in `config/banks/config-bankN.toml`, lists `0x801B3008` and the recompiler emits **no** yield at a listed backward branch. Regenerating changes **exactly one file** (`BankNFuncs/funcs_3.c`, one line removed; 52 → 51 sites; the other 144 generated files byte-identical by SHA-256), and the boot's scene timeline is unchanged. **Developer-confirmed fixed**: driven to a battle at `OGRE_SPEED=8` the mission runs normally (0 slow seconds, `0x801B2F4C` never hot, p50 6 ms / p99 31 ms display-list intervals). Reusable rule: for a misclassified loop, list the branch — do not weaken the heuristic. |
| [HANDOFF-2026-09-18-session79.md](HANDOFF-2026-09-18-session79.md) | *(Superseded by session 80 — the repair is landed.)* Session 79 handoff — **the mission's post-battle lag is a recompiler-injected `yield_self` the game executes once per entity — cause found, repair NOT landed.** The developer's *"after a battle, when the enemy unit's destruction effect ends, it lags badly — it draws and responds, just very slowly"* is measured: submitted display lists go from 33 ms apart to ~717 ms (**30 → 1.4 lists/s**) inside scene `0x03`, while `processDisplayLists` stays at 4–6 ms and the log has **0 stub calls and 0 `UNKNOWN module`** — the renderer and the bank map are healthy and the game thread is *waiting*. `OGRE_PROFILE=1` puts the frame-pump thread `t4` in **`0x801B2F4C` for 93–96 % of the slow seconds**, the mission module's nearest-target search, whose loop's backward branch (`0x801B3008`) carries `yield_self(rdram);` — and `yield_self` blocks until an external message arrives (≈ one VI retrace, 16.7 ms), so a ~22-entity scan costs 0.4–0.7 s per frame. N64Recomp's `is_yield_poll_loop` accepts it because a load in the body uses a base set once at function entry. **Three candidate repairs were built and all three stalled the boot**, which establishes that `yield_self` is also the only external-message pump a running thread has; all were reverted, so the tree and `tools/N64Recomp` are back at their recorded baseline. New measurement tools: `tools/run-lag.sh`, `tools/proflog.py --names`, `tools/symlook.py`. |
| [HANDOFF-2026-09-17-session72.md](HANDOFF-2026-09-17-session72.md) | Session 72 handoff — **combat and items run: eleven more arena banks, and the silent wrong-bank hang.** Driven interactively (the client logs to a file, the developer plays, the agent reads the log), with a new live-console **`dmatrace`** command that prints the accumulated streamed-DMA map on demand. The killer finding is a shape with **no diagnostic at all**: `on_streamed_dma` calls `load_function_bank` only for records it knows, so an unknown module is never **evicted** — the *previous* bank's body keeps running at those addresses (`jal 0x80217E50` ran unit Q → the **settings menu froze** with no stub), and where the address is not a function in any registered bank, `get_function` returns the logging stub (the **shop's** `0x80219594`, called **3553×**, once per frame → an empty screen). Enumerating every `jal 8009da50` arena load showed **record 9's arena at RAM `0x80214FA0` has thirteen banks**; the eight missing ones are now **Y, Z, AA, AB (shop), AC, AD, AE, AF**, alongside **V** (settings, `0x1A4BE0`), **W** (combat, `0x17F9E0`) and **X** (record 10's battle bank, `0x22A250`). Also: **unit P's** size corrected `0x6200 → 0x6030` (chunk-rounded; the old end marker swallowed `0x1D0` bytes of bank Y) and **P/Q BSS** zeroing added; `elfcheck`'s byte identity **cannot** catch an over-large module, so re-derive the loader. Final run: **0 stubs, 0 `UNKNOWN module`**, developer-confirmed (mission, combat, town shop, Organize Screen); the reported slowdowns are render-bound (`processDisplayLists` median 4.4 ms at `OGRE_SPEED=4`, whose budget is ~4.15 ms). `make bank-recomp`: **32 units, 42 records, 3452 functions**. |
| [HANDOFF-2026-09-17-session71.md](HANDOFF-2026-09-17-session71.md) | Session 71 handoff — **the tutorial's practice stage is bank unit T (and the arena's mode-2 bank is unit U).** The developer's *"open the tutorial … pick an option … we are placed in a map and nothing happens, without enemies"* is scene `0x03` entered with the mode word `D_80193700 != 0` — descriptor **`0x8018F364`**, mask `0x0004038C` (records 2,3,7,8,9 **and 18**), vs the normal mission's `0x8018F350`/`0x0000038C`. That path loads a **second bank of record 18's arena** — ROM `0x1C32D0` (`0x5D50`) → RAM `0x8022A860` — which the port had no code for: four call sites (`0x8022A860` unit-A record 3, `0x8022A88C` unit-N record 7, `0x8022AA94` the mode-table callback, `0x8022B36C` unit-A record 3) hit the runtime's logging stub, 4608 calls in a 40 s run. Record 18's own loader `func_ovlB_80221D50` (RAM `0x80221D50`) indexes a `0x28`-byte segment table at RAM `0x80229DDC` and a `0x1C`-byte mode table at `0x80229E88` by `D_80193700`, then `func_8009DA50(0x1C32D0, 0x8022A860, 0x5D50)` and `jalr`s `+0x08`; the module's data half is the **tutorial lesson text** (`Stationing unit will gather information.`, `This concludes the tutorial on Use Item command. Proceed?`), so the field was inert. Now **unit T** (three forced cross-record entries, BSS `0x802305B0..0x802305F0`) and **unit U** for mode 2 (ROM `0x1C9020`, `0x5020`, entry `0x8022A87C`; no observed route loads it, but it shares the RAM so the bank map must evict unit T). Verified with `OGRE_SCENE=tutorial` + scene-keyed A taps: `0x17 → 0x02 → 0x0D → 0x03 (0x8018F364)`, **0 stub calls**, the `TUTORIAL / These are called the Field` instruction box renders and the lessons advance (`docs/proofs/native-tutorial-practice-field.png`, `-command.png`); the `down down` lesson selects mode 0 (the normal mission) and is stub-free too. `make bank-recomp`: `21 unit(s), 31 record(s), 3155 function(s)`, `check-banks OK`. Developer-confirmed live: the tutorial instructions **and the enemy units** now appear on the practice field. |
| [HANDOFF-2026-09-17-session70.md](HANDOFF-2026-09-17-session70.md) | Session 70 handoff — **the port's *timeline* is wall-clock, but its *state* is reproducible to the byte**, measured before designing any test fixture. A temporary scene-armed, poll-counted script (title → `Load Game` → map → three cursor steps left) plus a sample of the map's `state` struct: **input polls are ~1:1 with VI retraces** (`min=1 max=2 mean=1.01`), which **corrects the "the game polls input far faster than 60 Hz" claim below** — a cursor step needs a long hold because the cursor integrates a small delta *per retrace*. Two runs at `OGRE_SPEED=4` differ **only** in the absolute clock (a 16–19 retrace boot offset that every later event keeps); normalised for it they are byte-identical at map entry and after the input (same hexdump, `dir`, `statehash80`), and a third run at `OGRE_SPEED=2` is byte-identical too except the free-running animation tick `state[+0x108]` (373 vs 374). The VI retrace clock is wall-clock (`events.cpp:237`), which is *why* absolute frame/poll indices desynchronise — so an emulator recording must be re-anchored **on game state**, and an emulator **savestate** is a data oracle to *read*, not to *load* (guest addresses are identical; `OSThread::context` host pointers are what cannot cross a process, session 65). For map/menu moments the durable build-independent fixture is the game's own `.srm` (`tools/sramsave.py`; title → `0x12` → `0x05`). Probe reverted; no files changed net. |
| [HANDOFF-2026-09-17-session69.md](HANDOFF-2026-09-17-session69.md) | Session 69 handoff — **the Organize Screen (scene `0x06`) is bank unit S.** Picking `Organize Screen` from the mission's `R` menu (or reaching the pre-battle screen on the `map → mission` route) entered scene `0x06` and bounced back to `0x03` **68 ms later** — the "the screen just reloads" the developer reported. The scene's enter `func_8017B6D0` DMAs **ROM `0x87220` (`0x56D60`) → RAM `0x8019A7C0` … `0x801F1520`** (no BSS — `bss_start == bss_end`) and `jal`s the module's entry `0x801C19B0`; the generic scene update `func_8017B858` (`*(0x801977E8) == 2`) and hook `func_8017B9C8` `jal` `0x801C214C` and `0x801B7FC0`. None was compiled, so all three hit the runtime's logging stub, the screen was never built and the state machine took its done arm. Now **bank unit S** (one record, three forced entries, three `dispatch --only` targets — the hook's target also exists as a real overlay-C function, which is exactly why it had to be dispatched). The second `[bank] UNKNOWN module` line in that log is a **false positive of the chunk-DMA heuristic** (chunk 151 of this DMA lands on `0x801AD5C0`; `0x9A020-0x87220 == 0x801AD5C0-0x8019A7C0`). Driving facts worth keeping: **the keyboard's `R` shoulder is `E`** (the letter `R` is unmapped), and the mission's `R` menu is a **hold** menu, so only a capture *during* the hold sees it. Developer-confirmed live; `docs/proofs/native-organize-screen.png`. |
| [HANDOFF-2026-09-17-session68.md](HANDOFF-2026-09-17-session68.md) | Session 68 handoff — **the mission intro works on the *natural* route (map → story → mission)**, which is not the same program as the suspend route: the natural route streams a **fourth bank** of record 9's arena — ROM `0x195430` (`0x2380`) → RAM `0x80214FA0`, now **bank unit R** — that the port had no code for, so unit N's record-7 loader (`func_ovlN_801AE880+0x764`, ROM `0x1036E4`: `func_8009DA50(0x195430, 0x80214FA0, 0x2380)` then `jal 0x80215C38`) landed on the runtime's logging stub. The module is the mission's enemy/unit constructor (`No free space on TCharacterEnemyData.`), so the target fort was built from stale bytes (garbled draw) and the intro waited forever — `A`, which advances the `NOTE`/`MISSION START` phases, did nothing. Now: 0 `UNKNOWN module`, 0 stub calls, the whole intro plays (`docs/proofs/native-mission-intro-natural.png`). Also landed two driving knobs: `OGRE_TAP_SCENE_BUTTON` (a **scene-keyed** tap schedule — the wall-clock one lands on the map in one run and in the attract loop in the next) and the live console's `press <buttons> [polls] [x] [y]` (synthetic pad + analog stick; ~~the game polls input far faster than 60 Hz~~ — **corrected in session 70: input polls are ~1:1 with VI retraces, so a few hundred polls is a few hundred frames and the cursor moves a little each one**). |
| [HANDOFF-2026-09-17-session67.md](HANDOFF-2026-09-17-session67.md) | Session 67 handoff — **the mission scene (scene `0x03`) renders.** The developer's suspend save (`assets/save-mission-1.srm`, third SRAM slot) resumes there from the title's `Load Game`, and it now enters, runs and draws (3D terrain + the 2D party sprite + tooltip/unit panels: `docs/proofs/native-mission-scene.png`, `-unit-panel.png`). Four swappable-RAM fixes: the three records it streams (segment table **7/8/9**, `0x101D00`/`0x145230`/`0x14EC00` → RAM `0x801AD5C0`/`0x801F4050`/`0x801FDA90`) are **unit N**; unit A's record 3 called `0x801AD6BC` — record 6's RAM — and, both records being in unit A, N64Recomp bound it to unit A's record-6 body, but scene `0x03`'s mask `0x38C` loads record 3 **without** record 6 (record 7 is resident), so **record 6 moved to unit O** and the call compiles as `LOOKUP_FUNC`; a `LOOKUP_FUNC` onto a **non-entry** is a silent no-op (`get_function` has no interior fallback) and a cross-*record* `jal` is invisible while the target record is disassembled, so the entries are declared in `symbol_addrs-bank{N,O,P,Q}.txt` (a subsegment split would insert the assembler's 16-byte `.text` padding and break the ELF-vs-ROM check); and record 9's arena streams **two more banks the segment table does not describe** — units **P** (ROM `0x171EC0`) and **Q** (ROM `0x165FE0`), both → RAM `0x80214FA0` — found from the port's stub log and a light `do_rom_read` probe because the full DMA trace perturbs the run onto the other bank. Also: **`tools/cross_bank.py check-banks` was a silent no-op** (its YAML parser dropped the `name:` on `- name: bankRecX` lines, so every record was `"?"` and the same-record test was vacuously true); it now parses names, uses the scene descriptor masks to tell "always loaded together" from a real split (1728 → 23) and allowlists the pre-existing unit-C backlog. |
| [HANDOFF-2026-09-17-session66.md](HANDOFF-2026-09-17-session66.md) | Session 66 handoff — **OB64's save is 32 KiB of battery SRAM and it now works end to end.** The chip is SRAM (mupen64plus's database `SaveType=SRAM` for this ROM's CRC, and the game's own code: `func_8008A040` builds the save `OSPiHandle` with `baseAddress 0xA8000000`, `func_80074CF0..` reads the whole image in 256-byte DMAs, `func_80074BF0` → `func_80074C58` writes it back). `entry.save_type = recomp::SaveType::Sram`, and the rule that makes it work is that **the device of a game-issued PI DMA is the `OSPiHandle` the `OSIoMesg` carries** (`pi_perform_dma` in `librecomp/src/pi.cpp`), not the address — the old handler read ROM for every transfer, so the boot save read pulled ROM bytes. Verified: blank battery formatted and written (32768 bytes, real `QuestOG3` structure, deterministic), restart accepted, garbled image rejected/repaired, and a converted parallel-n64 save makes the title's `Load Game` appear with the cursor on it (`0x04 → 0x12 → 0x05`, dev-confirmed). New `tools/sramsave.py` imports/exports emulator wrappings; new knob `OGRE_PREF_DIR`. |
| [HANDOFF-2026-09-17-session65.md](HANDOFF-2026-09-17-session65.md) | Session 65 handoff — **the map's sprites are fixed, and the cause was the linker, not the game.** Unit M's `.data` subsegment was linked **8 bytes above** its ROM address (`mips-linux-gnu-as` pads `.text` to 16 bytes; the module's code/data boundary ROM `0x85838` = RAM `0x801A68A8` is only 8-byte aligned), so **150 of the unit's 480 address-named symbols were +8** and every `lui/%lo` data read in the recompiled module was 8 bytes high. The map's sprite table is `D_ovlM_801A6FD8` (raw ROM `0x7E804` = `addiu v0,v0,0x6FD8`; the bank ELF said `0x6FE0`), so **every sprite descriptor was read one entry late** — which is exactly the "the two calls name each other's entries" of sessions 61–63: `a2=10` = `(32,32)` = the party (the full knight frame), `a2=11` = `(16,11)` = the shadow, `a2=12` = `(144,23)` = the date plate, `a2=6` = `(16,16)` = the cursor. **Fix, config only:** the `asm` subsegment now ends at `0x85840` (the 8 zeros there are the module's own padding; nothing references `0x801A68A8`), so the assembler's pad is real bytes and every label lands on its ROM address. Result: the party is a 32x32 knight with its 16x11 shadow, one 24x23 cursor, and the full `MONTH/DATE \| Sombra \| 1` plate — `docs/proofs/native-newgame-map-scene.png` **replaced** (the broken render kept as `map-sprites-before-offby8.png`). **New guard:** `tools/elfcheck.py` / `make elf-rom-check` compares every ELF byte-for-byte with the ROM and audits address-named symbols — **all 13 bank ELFs and the main ELF are now byte-identical to the ROM**; `make bank-recomp` runs it before recompiling. **Session 64's `line = 8` lead is withdrawn:** for RGBA32 the RDP keeps 2 bytes per texel per TMEM half, so `line = 8` is 32 texels per row — the sheet's row — and RT64's loader/sampler match angrylion's `get_tmem_idx`. |
| [HANDOFF-2026-09-17-session64.md](HANDOFF-2026-09-17-session64.md) | *(Its renderer-side `line = 8` lead and its sprite-entry table (entry 11 `(144,23)` for `a2=0xB`, entry 10 `(16,11)` for `a2=0xA`) are **superseded by session 65** — those readings came from the bank ELF, whose data labels were 8 bytes high.)* Session 64 handoff — the map screen, **reframed**: *"how would an emulator handle it?"* **No code landed.** Four independent checks say the port is faithful — the LZ decoder is exact (13/13 assets end **on** their declared payload boundary), the port's `state[+0x04]` buffer is **21128/21128 bytes identical** to an offline decode of asset `0x01DD210A`, scene `0x05`'s ucode `0x8009F540` hashes (XXH3-64, raw, len `0x1390`) to RT64's **`F3DEX2.fifo 2.08`** DB entry so the GBI choice is right, and every constant was re-read out of the raw ROM. **Two session-61/62 mechanisms are withdrawn:** N64Recomp does *not* run a `jal` delay slot twice (the duplicate is dead code after `goto after_N`), and the builder emits **no** `G_SETTILESIZE` — the word at `0x8019F990` is the TEXRECT's **s,t**, so there was never a second window writer. **New model:** the knight is not an asset — the map's enter `malloc(0x18000)`s `state[+0x34]` and **composites** an RGB555 LUT over an 8-bit index image into it: a **32-texel-wide** RGBA32 sheet, **24 frames** of `0x1000` (`3*state[0x1DC] + f`, 8 directions x 3 frames); `state[+0x04]+0x1068` really is zero and the only translucent-black shadow art in any map asset is at `+0x10AC` of that same asset; `state` is a **heap pointer** (`*(0x80197B18)`). See `docs/guides/emulator-first.md`. |
| [HANDOFF-2026-09-16-session63.md](HANDOFF-2026-09-16-session63.md) | Session 63 handoff — **negative, nothing landed**, but the party's two draws are identified: the developer corrected sessions 61/62 — the **second** builder call (`a2=0xA`, rect `(a0-16, a1-24)`, i.e. the rect *above*) is the **knight (Magnus)** and must be `16x24`; the **first** (`a2=0xB`, rect `(a0-8, a1)`) is the **shadow** and must be `16x11`. The two calls therefore name each other's entries (live: slot 12 = `(16,24)`, slot 10 = `(16,11)`, slot 11 = `(144,23)`), which is why the shadow drew as the row of ellipses and the knight as a dark blob. The window mechanism is settled (the caller's `jal` delay-slot store lands on the builder's own `dl+0x3C` word and wins on **hardware** too — no port artifact, no second writer). **The wall:** with rects and per-entry windows consistent, both sprites are clean dark blocks because the texture buffer the knight samples — `0x8021AC88`, pointer faithful to the emitted list — is **all zero** in RDRAM (`[probe70] call1 tex=8021AC88 w0=00000000`), and a zero RGBA32 texture under the map's alpha combiner renders flat. Next: find which pass fills that buffer, or which field of `0x801F1570` names a populated one. *(Its "delay-slot store wins on hardware too", its `0x801F1570` field addresses and its shadow `+0x10E0` are corrected by session 64.)* |
| [HANDOFF-2026-09-16-session62.md](HANDOFF-2026-09-16-session62.md) | Session 62 handoff — **negative result, nothing landed.** *(Its table printout and its "N64Recomp lets the caller's window win" framing are corrected by session 63; the mechanism it identified is right.)* The map's `G_SETTILESIZE` window question from session 61 is answered at instruction level: the window is the **caller's `jal` delay-slot store** (`func_ovlM_801A2A7C`, ROM `0x81B44` `sw $t5, 0x44($v0)`, `$t5 = 0x1C028` = a `28x40`-texel window), written to a different display-list slot than the builder's own window; N64Recomp emits a delay-slot store **before** the call as well as after it (the `goto after_N` shape), so the caller's constant wins and the callee's window is discarded — hence a `144x23`-px rect wrapping the atlas, i.e. the row of ~18 ellipses. The fix derived from this (window from the entry's `(W,H)`, body entry `0xB`->`0xC`) **regressed a better mid-session probe frame and was reverted**; the developer's oracle is that the party is **two sprites** — a `16x24` knight and a **static soft oval shadow below it**. §0 has the honest status. |
| [HANDOFF-2026-09-16-session61.md](HANDOFF-2026-09-16-session61.md) | Session 61 handoff: **the map's sprite *sizes*** — the party marker (Magnus) and the cursor draw the **wrong sprite-table entry**. Corrects session 60 §7's "a descriptor the map never finished filling": the `7x10`-texel `SETTILESIZE` **is** in the game's own display list, the sprite table (`0x801A6FE0 + a2*8`) is static and byte-identical to unit M's ROM data half, and the builder `func_ovlM_8019F83C` is faithful — an emitted rect always equals the entry's `(W,H)`. The party draw (`func_ovlM_801A2A7C`, ROM `0x81B50`) asks for slot **11** (`144x23`, a HUD-width entry) while the knight's four frames are slots **12-15** (`16x24`); the developer measured retail's marker at ~10% of screen height / 5% of width = **16x24 px**. Proof by A/B: a global `base+8` made the party exactly `16x24` (but shifted every other sprite, so it is not the fix); a targeted `0xB→0xC` shrank the bar to a compact sprite. Open: the emitted window stays `7x10` even for a `(16,24)` entry, so the window's writer is still unidentified. |
| [HANDOFF-2026-09-16-session60.md](HANDOFF-2026-09-16-session60.md) | Session 60 handoff: **the map scene (scene `0x05`) renders** — it was black because *no frames were produced*. Two calls in the scene update/hook (the update's `jal 0x8019AF0C` @0x8017B8A0, the hook's `jal 0x801A103C` @0x8017BA10) are into RAM the scene module occupies, and N64Recomp had compiled each as "call the containing body + early `return`" (its emission for a `jal` into a size-overridden body interior). The early return abandons the caller's frame, so the frame-pump thread read its saved `$s0`/`$s1` from the wrong stack slots; `$s1` is the message-type comparator, became 0, and the type-1 dispatch stopped matching — the pump died after two frames (`D_800AEFA4` frozen while `D_800C4BCC` still counts; 2 display lists). Both targets are now in `make recomp`'s `cross_bank.py dispatch --only` list, which also triggers the tool's tail-call repair (call-and-continue); `0x8019AF0C` resolves to unit **M**'s state-1 handler. Verified: forced `OGRE_SCENE=0x05` 2 → **124 display lists**; the natural route submits **2296** and the map draws (`docs/proofs/native-newgame-map-scene.png`), with the movie and Prologue card unchanged. Also records the diagnostic (log guest `$sp` around each call to find a leaking one) and the no-`--only` full-dispatch experiment (89 sites, 7 extra targets — also works; the way to clear the backlog). |
| [HANDOFF-2026-09-16-session59.md](HANDOFF-2026-09-16-session59.md) | Session 59 handoff: **the sequence-end crash is fixed and the opening plays past the movie.** The crash was a missing **third bank** of the record-14 arena: the chapter-animation step (1073, command **`-8`** — `func_ovlC_80227E64` maps the descriptor's last-word low byte through the table at `0x8022ABE0`) streams **ROM `0x286BA0` (0x138F0) → RAM `0x8022ACB0`**, and `bankRec14a` — the *other* bank, compiled into unit C — had unit C's calls into that RAM (`jal 0x8022C270`/`0x8022C6E4` from the shared interpreter, `jal 0x8022E3F0` from the command-`-8` callback) bound to its bodies, so the interpreter ran rec14a's layout, left the engine state (`0x8022A970`/`78`/`94`) at 0 and walked guest 0. Fixed with **unit K** (chapter module) + **unit L** (`bankRec14a` moved out of unit C), plus `bankRec14a`'s ROM end (`0x2A8CF0`/`0xE860`, not `0x2A82F0`/`0xDE60` — the truncated `0xA00` held its jump tables). Result: the **Prologue card renders** (`native-newgame-prologue-card.png`), the post-movie story renders (General Godeslas `-received-for-duty.png`; Magnus `-magnus-old-man.png`), and the sequence reaches scene `0x05` with **0 stub calls**. Scene `0x05`'s module is **unit M** (ROM `0x79750` → RAM `0x8019A7C0`; unit H's over-claimed `0x8600` is corrected to `0x84B0`), and `is_known_module` now compares the chunk's rom→ram delta. Also corrects session 58's global addresses (`0x8022A9xx`, not `0x8023A9xx`), `func_80227700` (it only stores the selector) and step 1073's command. *(Scene `0x05` itself rendered black until session 60.)* |
| [HANDOFF-2026-09-16-session58.md](HANDOFF-2026-09-16-session58.md) | Session 58 handoff: **checkpoints and the closing movie.** The live console gains `save`/`load` — a checkpoint is the whole 8 MiB RDRAM image **plus the runtime's overlay state** (which recompiled body is mapped at each RAM address), and both wrap the file I/O in a thread-park (`ultramodern::checkpoint_pause_begin/end`) because the N64 threads are 1:1 native and the image otherwise **tears** (first attempts wrote a file whose own checksum did not match its bytes). Verified: self-consistency, in-run rewind + replay forward, cross-process load. **Scene `0x16`'s streamed module is compiled** (bank unit **I**: ROM `0x244770` → RAM `0x801D0860`, 43 functions; `bankRec10a` moved out of unit C into unit **J** as the other bank of that RAM), so the closing movie plays and the sequence advances out of it. The developer's **sequence-end crash now reproduces** (deterministic `SIGBUS` in `func_ovlC_8022C270 + 0x53A`, faulting guest `0x7FFF43E8`) with `D_8018F1C0 = 0x0431` — step 1073, past the decoded 19-step table. Also three build-hygiene fixes (clear a bank unit's asm/assets before `splat split`; clear the generated trees before regenerating; an explicit `bin` gap after a `data` subsegment) and `gen_bank_syms.py` now sees `.Lovl<U>_<addr>` references. *(Its crash mechanism and its `0x8023A9xx` addresses are corrected by session 59.)* |
| [HANDOFF-2026-09-16-session57.md](HANDOFF-2026-09-16-session57.md) | Session 57 handoff: **the stale New Game backdrop is the njpeg readback's source *selection*, not a render-vs-readback race.** The old patch preferred RT64's never-cleared scratch word `+8`, which at the first pass of every assembly still named the previous step's framebuffer — by then holding the previous screen (the name/date-of-birth form): 13 of 120 measured stage-3 passes read the wrong sub-image before, 0 after, by keeping the game's own `state[0x64]` when `D_800C4BB8` matches a framebuffer-table entry. The ordering hypothesis is wrong too — the game waits for the **DP** completion (`events.cpp:432`), so an in-flight-display-list handshake never blocked once and was reverted. Also: scene `0x16` ends at an **uncompiled module** (ROM `0x244770` ≈`0x7500` → RAM `0x801D0860`, a `bin` gap in `config/banks/config-bankC.yaml` while `bankRec10a` owns that RAM), so the game spins on the streamed stub; the sequence-end crash is still not reproduced (200 s, exit 0). |
| [HANDOFF-2026-09-15-session44.md](HANDOFF-2026-09-15-session44.md) | Session 44 handoff: the New Game **step table** is decoded (steps 1..19 → asset id, ROM, command; every step ≥ 2 selects selector 2); step 2's crash was two stores to guest `0`/`8` (the `0x80239874` shared tail's `*(sp+0x1EC)`, and the descriptor interpreter's emitter with `a3 = 0`), and every value feeding them comes from game code — so retail must tolerate them. **Fix: the N64's low-window (KUSEG) RDRAM alias restored in `recomp_mem_addr`** (`a < 0x80000000` → `a & 0x003FFFFF`, recorded in `patches/n64modernruntime-n64recomp.patch`) — the step-2 enter now completes; the next wall is RT64-only (`do_sendP + 0xC4`, guest `0xFE6E2C89`). `tools/ogrelz.py` gained `--asset` and the rule that an asset's LZ block starts at `rom+4`. |
| [HANDOFF-2026-09-15-session49.md](HANDOFF-2026-09-15-session49.md) | Session 49 handoff (latest): **the cathedral background is still black.** A YUV16 decoder was written for RT64's `TextureDecoder.hlsli` (its `G_IM_FMT_YUV` arm does return black) and **reverted** — measured against the pristine shader it turns the frame into a corrupt magenta/green blob, not the backdrop. The real measurement: the game's CPU readback (`0x80199884`) runs 4×, all in scene `0x02`, and **every copy reads a zero framebuffer**, which is upstream of any decode. Next lead: check GLideN64 (which upstream fixed [issue #102](https://github.com/mupen64plus/mupen64plus-user-issues/issues/102) with) before writing another decoder. Includes the `OGRE_DUMP_RDRAM` A/B recipe (`OGRE_CAPTURE_PRESENT` is too sparse for this) and a refreshed `patches/rt64-ob64.patch`. |
| [HANDOFF-2026-09-15-session52.md](HANDOFF-2026-09-15-session52.md) | Session 52 handoff (latest): **the cathedral background renders — the YUV→RGB conversion matrix was never applied.** The backdrop drew as blue banding because RT64 fed `G_SETCONVERT`'s **raw unsigned 9-bit** `k0..k3` fields into the matrix, while the hardware sign-extends each field and scales it `2*K+1` (GLideN64 `gDPSetConvert` `SIGN(k,9)<<1+1`, parallel-rdp `set_convert` `2*sext<9>(k)+1`), **and** the green row paired `K1` with V instead of U (`G = Y + K1*U' + K2*V'`, `R = Y + K0*V'`, `B = Y + K3*U'`). With `K0=175 K1=469 K2=423 K3=222` the corrected coefficients are 351/−85/−177/445. The geometry, the de-interleaved TMEM planes and the UYVY source were all verified correct (a loader probe dumped all 16 rows), so session 51 §7's "the defect is in sampling/upload" is wrong — the blue was the raw-coefficient matrix exploding green through the game's own `(TEXEL0-K4)*K5+TEXEL0` combiner. Proofs `native-newgame-cathedral.png` / `-cathedral-background.png` **replaced** (the old ones were the black/blue-band broken renders); the assembled `'B5'` image at `0x80243E28` now renders as the cathedral. |
| [HANDOFF-2026-09-15-session51.md](HANDOFF-2026-09-15-session51.md) | Session 51 handoff: **the cathedral background's missing geometry is found and implemented.** The `0x800A5110` njpeg ucode is **`S2DEX 2.08`** (`GBIUCode::S2DEX2` — the game's ucode text/data hashes match RT64's `S2DEX2_FIFO_2_08` entries exactly), so its per-macroblock `0xDC`/`0xDA` commands are **`G_OBJ_MOVEMEM` (`gSPObjSubMatrix`)** and **`G_OBJ_RECTANGLE_R` (`gSPObjRectangleR`)** — the draw — not F3DEX2 `G_MOVEMEM`/`G_MTX`. RT64's `GBI_S2DEX2` mapped neither, so every macroblock was loaded into TMEM and never drawn and the readback stayed empty; **session 50 §9's "the list carries no geometry" is wrong**. Both commands (plus `G_OBJ_SPRITE`/`G_OBJ_RECTANGLE` and the four `objLoadTx*` handlers that `assert(false)`'d) are implemented, and a live trace shows **300 rectangles tiling the 320x240 `G_SETCIMG` target exactly**. The YUV16 tile is re-done as the RDP's two-plane format (a YUV `LOADTILE` de-interleaves luma into TMEM's upper half and U/V into the lower; parallel-rdp's `sample_texel_yuv16`; YUV16 needs raw-TMEM sampling) — session 50's sampler read luma from a half the load never wrote, so it could never work — and the source word is `[U, Y(even), V, Y(odd)]` (correcting session 50 §2). **Still open: the sampled colours** (the backdrop draws as blue banding), with the geometry, the RDRAM source and the TMEM planes all verified correct; §7 ranks the next checks. *(The colours are fixed by session 52 — the `G_SETCONVERT` coefficients and the green-row channel pairing; §7's "the defect is in sampling/upload" is corrected there.)* |
| [HANDOFF-2026-09-15-session50.md](HANDOFF-2026-09-15-session50.md) | Session 50 handoff: **RT64's YUV16 decode is implemented — the black backdrop's first cause is fixed.** `TextureDecoder.hlsli` returned `float4(0,0,0,1)` for every `G_IM_FMT_YUV` texel and the njpeg background is drawn as `fmt=1 siz=2`, so the framebuffer the game reads back was black by construction. The layout comes from the code that writes it (mupen64plus-rsp-hle `jpeg.c` `GetUYVY`): `byte0 = Y(s even)`, `byte1 = V`, `byte2 = U`, `byte3 = Y(s odd)` in the word at `4*(2t + (s>>1))`, luma from the upper TMEM half, chroma from the lower; the conversion is the matrix **the game itself programs** (`G_SETCONVERT`, `k0=175 k1=469 k2=423 k3=222`). The remaining wall is render **timing**: the port completes the emulated RSP task when the display list reaches RT64, so the CPU copy can run before the draw renders; `Application::waitForGameFramebuffers` (RSP worker, `OGRE_NJ_WAIT_MS`) bounds that wait, and a wait on the *game* thread deadlocks. Follow-up static trace (§5) confirms the four stage-3 copies **are** the njpeg readback — sole caller bankE `0x80199D80` inside `func_ovlE_80199D30`'s drive loop; the four sizes are the four sub-images of one `'B5'` asset (ROM `0x7CADAC`), preloaded during scene `0x02` because `0x0D` streams a different bank over that RAM. The readback that matters is therefore the scene-`0x02` one, and the open question is why the `0x800A5110` draw produces no game framebuffer for the wait to see. *(§2 and §9 are corrected by session 51.)* |
| [HANDOFF-2026-09-15-session47.md](HANDOFF-2026-09-15-session47.md) | Session 47 handoff: **the njpeg decoder was mis-labelled by RSPRecomp**. `text_address` is a *label base* — it must be the RSP IMEM DMA address the microcode was assembled for (`0x1080`; the boot loader loads the text at IMEM `0x080`), not the text's RDRAM address. With `0x8009ED80` every `j` target resolved `0x300` bytes late, so the decoder wrote one `0x300` block per task; with `0x1080` all `mbs` blocks decode (all four cathedral images, `Broke`, 0–1 ms) and it is **on by default** (`OGRE_NJPEG=0` forces the stub). The pipeline is documented end to end (CPU Huffman decode `func_8008B250` → this microcode → a *second* gfx ucode `0x800A5110` drawing 16x16 YUV16 macroblocks → CPU framebuffer readback → a `'B5'`-headed image at `0x80243E10` with pixels at `0x80243E28`). **The background is still black** (see session 49), because the readback copies a zero framebuffer. |
| [HANDOFF-2026-09-15-session46.md](HANDOFF-2026-09-15-session46.md) | Session 46 handoff: the cathedral scene's **missing background**. The background is a 320x240 pre-rendered image at guest `0x80243E28` (the first 48 `G_TRI2` quads of the step-2 display list); `func_ovlE_80199D30` assembles it from a resource the game decodes through **four RSP `M_NJPEGTASK` (type 4) tasks**, which the port stubs. The microcode is recompiled (`make rsp-recomp` → `RspFuncs/njpeg_ucode.cpp`; ucode `0x8009ED80`, boot `0x8009ECB0`, tables `0x800AC050`) and dispatchable behind `OGRE_NJPEG=1`, but it stops after one `0x300`-byte block, the scene then stops drawing, and the null build dies in the runtime's message queue (null queue `0x80000010`) — so the **stub is still the default and the background is still missing**. *Superseded by session 47 on the decoder bug, on the task input (`func_8008B250`) and on the `0x80243E28` assembly.* |
| [HANDOFF-2026-09-15-session45.md](HANDOFF-2026-09-15-session45.md) | Session 45 handoff: step 2's wall was a **missing streamed module** — scene `0x0D` streams `bankRec14c` (ROM `0x2A8CF0`) over `bankRec14b` at RAM `0x802395E0` for steps ≥ 2, and the port had only rec14b, so unit C's 95 calls into that arena ran rec14b's prologue-less bodies (frame leak `+0x238`, `s3`/`s6` = 0, the interpreter walks guest `0`, RT64's ~2.5 GB DMA and `do_send` SIGBUS). **Fix: new units F (rec14b) and G (rec14c)** so those calls compile as `LOOKUP_FUNC` and the DMA selects the resident bank. Result: the **cathedral dialogue with Archbishop Odiron renders** on both renderers (`native-newgame-cathedral.png`). Session 44's KUSEG-mirror justification is superseded. |
| [HANDOFF-2026-09-15-session43.md](HANDOFF-2026-09-15-session43.md) | Session 43 handoff: bank unit B compiles records 17/18 and scene `0x17` renders as the **Tutorial** (the title menu is captured: New Game / Tutorial / Stereo); the **first `0x0D` visit is the New Game movie and already renders** (sepia courtyard cutscene, "I promise I'll make you proud."), so the wall is **step 2** — the cathedral dialogue with `Archbishop Odiron` the developer identified; the movie-path word is `0x80197794`, not `0x8019F794`; dialogue text is LZ-compressed (`tools/ogrelz.py`). |
| [HANDOFF-2026-09-14-session35.md](HANDOFF-2026-09-14-session35.md) | Session 35 handoff: the title screen's white band — six `G_TEXRECT`s drawn with a zero-texel tile, and the guard that skips them; the `OGRE_NOP_RECT` bisect tool; the RT64 patch regenerated. |
| [HANDOFF-2026-09-12-session34.md](HANDOFF-2026-09-12-session34.md) | Session 34 handoff: the intro's missing smoke and 3D logo characters (a stale linker script meant overlay C was never loaded) and the narrowed bank-swap eviction; cross-bank `jal` routing built but blocked. |
| [HANDOFF-2026-09-12-session33.md](HANDOFF-2026-09-12-session33.md) | Session 33 handoff: the streamed-overlay segment table and bank descriptors decoded; banks are recompiled as a second unit and swapped on the game's DMA; the 170s run completes. |
| [HANDOFF-2026-09-12-session32.md](HANDOFF-2026-09-12-session32.md) | Session 32 handoff: the "Licensed by Nintendo"/ATLUS/QUEST stills render full-frame (a mis-named `osViSetMode` kept the game's 640x480 VI mode from the runtime); the ~95s crash is confirmed to be a streamed-overlay **bank swap**. Sessions 12–31 are in `docs/` (session 31: the intro renders — 205 display lists, ~28fps). |
| [HANDOFF-2026-09-12-session25.md](HANDOFF-2026-09-12-session25.md) | Session 25 handoff: native runs drive themselves (`OGRE_TAP_MS`/`OGRE_EXIT_AFTER_MS`); the idle trajectory reproduced on native and localised to the RSP/frame worker queues. |
| [WEB-PORT.md](WEB-PORT.md) | The WebAssembly / browser port plan (null renderer first, renderer decision later). |
| [WEB-PORT-REPORT.md](WEB-PORT-REPORT.md) | Web port implementation report (audit, RT64 deps, Emscripten issues, file changes). |
| [WEB-PORT-DEPLOYMENT.md](WEB-PORT-DEPLOYMENT.md) | Browser deployment requirements (COOP/COEP headers, local server). |
| [LIBULTRA-BRIDGING.md](LIBULTRA-BRIDGING.md) | The libultra-bridging plan: findings, identification methodology, seed symbol table, staged steps. |
| [guides/njpeg-backgrounds.md](guides/njpeg-backgrounds.md) | The game's **njpeg backgrounds** (the New Game cathedral and the ones like it): the asset format (`'HU'` + `u16` width/height + `'HUFF'` + macroblocks — all **75** assets enumerated, **42 of them 320x240**), the decode/draw/four-pass CPU-readback pipeline, the tile geometry (and why the framebuffer's chunk order is not the scene's), the contiguous-320x240-framebuffer trap, and how to check another one with `OGRE_NJREAD_LOG=1`. |
| [guides/app-build.md](guides/app-build.md) | How to build and run the PC port app — every `OGRE_*` knob, and the **diagnostics toolkit** (`tools/runlog.py`, `tools/guestmap.py`, `tools/rdram.py image`/`diff`, `tools/watch.sh`, `tools/scenemap.py` — the 25 scene types, every scene transition, and the step table; `tools/arenamap.py` — every streamed arena bank from the loader pattern, and whether a unit links it; `tools/stubmap.py` — which dispatched addresses the runtime's function map cannot resolve, ranked, with the callers that point at them, and a `log` mode that attributes a run's `streamed function stub` hits to the resident bank by matching the live bytes; `tools/recompcov.py` — how complete the recompilation is: code bytes, modules and dispatch offline, plus `OGRE_COVER=1` execution coverage per run). |
| [guides/app-architecture.md](guides/app-architecture.md) | App structure, runtime flow, callback responsibilities. |
| [guides/rsp-microcode.md](guides/rsp-microcode.md) | RSP microcode research and recompilation notes. |
| [guides/emulator-first.md](guides/emulator-first.md) | **Read before theorising about what a display list "meant".** The three pillars an emulator has (GBI identification by ucode hash, a faithful RSP, ground truth), where the port stands (it has the first, stubs the second, lacks the third), the five-check checklist to run before blaming game data, the recipe to hash a ucode into RT64's GBI database, how to borrow a reference emulator (RetroArch + Mupen64Plus-Next is installed), and what GLideN64/mupen64plus-rsp-hle know about OB64 specifically (`[OGREBATTLE64] enableTexCoordBounds=1`, `hack_Ogre64`, `jpeg_decode_OB`). |
| [guides/linux-migration.md](guides/linux-migration.md) | Moving the dev environment to Ubuntu: prerequisites and macOS-specific code to remove. |
| [guides/web-probes.md](guides/web-probes.md) | Headless-browser probes (`debug/`): serving the wasm build, running the probes, the page contract. |
| [PLAN.md](../PLAN.md) | The high-level project plan and roadmap. |

## Conventions

- **If you are an AI agent, read [`../AGENTS.md`](../AGENTS.md) first.** It holds
  the working rules this project learned the hard way; the most important one is
  to ask the developer what a scene should display instead of inferring intent
  from the code.
- ROM dumps / extracted assets are **never** committed to this repository.
- Third-party tools live under `tools/`. Only `tools/RT64` is a git submodule;
  `tools/N64Recomp`, `tools/N64ModernRuntime` and `tools/RecompFrontend` are
  gitignored vendored clones. The app project lives under `app/`.
- Our own debugging/verification tooling lives under `debug/` (the headless
  browser probes); it is not part of the app build. See
  [guides/web-probes.md](guides/web-probes.md).
- Local patches to upstream tools are kept as `*.patch` files under `patches/`
  (`n64recomp-ob64.patch`, `n64modernruntime-ob64.patch`,
  `n64modernruntime-n64recomp.patch`, `rt64-ob64.patch`,
  `rt64-plume-ob64.patch`); `tools/rt64-plume-sdl.patch` is the separate SDL
  ≥ 2.0.22 guard for older systems. Apply a patch to the submodule or the
  vendored clone — never commit modifications *inside* a submodule (a submodule
  only records an upstream commit SHA, so such changes are invisible to this repo).
- Code is written against the `N64ModernRuntime` (ultramodern + librecomp)
  callback interfaces; platform I/O (window, input, audio) is isolated in the
  app so the game code never touches SDL directly.
- **Document discoveries and decisions as you go.** As a session progresses, record
  findings, root causes, and choices in `DECISIONS.md` (top entry) and write a
  self-contained handoff (`HANDOFF-YYYY-MM-DD-sessionN.md`) at session end so a fresh
  context can continue without re-deriving anything. Vendored (gitignored) code changes
  are not committed, so list them explicitly in the handoff's "Changes" section.
